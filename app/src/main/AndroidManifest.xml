<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCDroid"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AfterLoginActivity"
            android:label="@string/logged_in_title" />
        <activity
            android:name=".InventoryImportActivity"
            android:label="@string/inventory_import" />
        <activity
            android:name=".MasterDataSyncActivity"
            android:label="@string/master_data_sync_activity" />
        <activity
            android:name=".AppConfigActivity"
            android:label="@string/configuration" />
        <activity
            android:name=".ArticleSync"
            android:label="Ürün Aktarımı" />
    </application>

</manifest>